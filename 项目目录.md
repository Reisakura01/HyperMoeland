# Islora 项目目录

> Islora（Windows 11）· VS2026 / .NET 10 / WPF（无 WinUI）· Windows SDK 10.0.26100
> 生成日期：2026-08-31 · 共 26 个文件

---

## 一、目录结构

```
Islora/
├── Islora.sln
├── README.md
├── .gitignore
└── Islora/
    ├── Islora.csproj    # 工程文件：net10.0-windows10.0.26100.0 + UseWPF + UseWindowsForms
    ├── App.xaml                      # 应用入口 XAML + 主题画刷初始定义
    ├── App.xaml.cs                   # 应用入口代码（StartupUri 启动 MainWindow）
    ├── MainWindow.xaml               # 岛窗口 XAML（透明、置顶、不抢焦点）
    ├── MainWindow.xaml.cs            # 岛窗口逻辑：云母应用 + 动画 + 服务接线
    ├── Core/
    │   ├── IslandState.cs            # 状态枚举（Compact/Expanded）+ 尺寸常量
    │   └── IslandController.cs       # 状态机（切换/收起 + 事件广播）
    ├── Models/
    │   └── MediaSessionInfo.cs       # 媒体会话快照（曲名/歌手/播放状态）
    ├── Theme/
    │   ├── AppTheme.cs               # 主题枚举（Day / Night）
    │   ├── ThemeManager.cs           # 主题画刷资源切换（前景/次要/边框/覆盖）
    │   └── ThemeScheduler.cs         # 按时间自动切换日间/夜间（默认 6:00–19:00）
    ├── Interop/
    │   ├── NativeMethods.cs          # Win32/DWM P/Invoke：云母、暗色模式、圆角、样式、显示器、前台窗口
    │   ├── MicaController.cs         # 应用云母 + 主题 + 不抢焦点
    │   └── MonitorHelper.cs          # 主屏 / 指定屏工作区查询
    ├── Services/
    │   ├── MediaService.cs           # SMTC 媒体会话（曲名/歌手）
    │   ├── NotificationService.cs    # 系统通知监听（UserNotificationListener）
    │   ├── BatteryService.cs         # 电量百分比
    │   ├── ForegroundWatcher.cs      # 全屏检测 + 多显示器跟随（500ms 轮询）
    │   └── TrayIcon.cs               # 系统托盘图标（退出入口）
    └── Views/
        ├── CompactPill.xaml          # 紧凑态胶囊 XAML（圆角胶囊）
        ├── CompactPill.xaml.cs       # 胶囊逻辑：时钟/媒体显示、点击事件
        ├── ExpandedCard.xaml         # 展开态卡片 XAML
        └── ExpandedCard.xaml.cs      # 卡片逻辑：时间/媒体/通知/电量显示
```

## 二、文件说明

| 文件 | 说明 |
|---|---|
| `Islora.sln` | 解决方案文件 |
| `README.md` | 项目说明、功能清单、云母排错 |
| `.gitignore` | git 忽略规则（bin/obj/.vs 等） |
| `Islora.csproj` | 工程配置：net10.0 + WPF + WinForms（托盘用） |
| `App.xaml` / `App.xaml.cs` | 应用入口 + 主题画刷默认值 |
| `MainWindow.xaml` / `.cs` | 核心窗口：透明置顶 + 云母 + 动画 + 服务接线 |
| `Core/IslandState.cs` | 状态与尺寸常量（190×46 / 420×300） |
| `Core/IslandController.cs` | 紧凑/展开状态机 |
| `Models/MediaSessionInfo.cs` | 媒体数据模型 |
| `Theme/AppTheme.cs` | Day/Night 枚举 |
| `Theme/ThemeManager.cs` | 画刷资源动态替换 |
| `Theme/ThemeScheduler.cs` | 按小时自动切换主题 |
| `Interop/NativeMethods.cs` | 全部 Win32/DWM P/Invoke |
| `Interop/MicaController.cs` | 云母应用入口 |
| `Interop/MonitorHelper.cs` | 显示器工作区查询 |
| `Services/MediaService.cs` | 全局媒体会话（SMTC） |
| `Services/NotificationService.cs` | 系统通知监听 |
| `Services/BatteryService.cs` | 电量 |
| `Services/ForegroundWatcher.cs` | 全屏隐藏 + 多屏跟随 |
| `Services/TrayIcon.cs` | 托盘退出菜单 |
| `Views/CompactPill.xaml` / `.cs` | 胶囊视图（时钟/媒体 + 点击展开） |
| `Views/ExpandedCard.xaml` / `.cs` | 卡片视图（时间/媒体/通知/电量 + 点击收回） |

## 三、构建与运行

```powershell
# 1. VS2026 Community 打开 Islora.sln
# 2. 确认 .NET 10 SDK + Windows SDK 10.0.26100 已安装
# 3. 直接 F5（或 Ctrl+Shift+B 编译）
```

详细说明见 `README.md`（功能清单、云母排错、日夜时间调整）。

---

*目录结构以实际文件为准，本文件生成于 2026-08-31。*
